name: Cypress Tests

on: [pull_request]
  
jobs:
  Run Tests With Chrome
  steps:
    - name: Git Checkout
      uses: actions/checkout@v3.3.0
    - name: Install Dependencies
      run: yarn install 
    - name: Run E2E Tests 
      run: yarn cypress run
